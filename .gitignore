# Дубль сборки под старым именем — нужен локально для републикации артефакта
# по уже выданной ссылке, в репозитории избыточен (index.html идентичен).
master-plan.html

node_modules/
.DS_Store
Thumbs.db
